export enum routes {
  dashboard = "/",
  login = "/spotify-login",
  fromPlaylist = "/from-playlist",
  fromSong = "/from-song",
  fromGenre = "/from-genre",
  statistics = "/statistics",
  episodes = "/episodes",
}
